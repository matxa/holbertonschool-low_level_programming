#include "holberton.h"

/**
 * _abs - checks if c is lowercase
 * @n: is a variables
 * Return: abs
 *
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (n);
	}
	else
	{
		return (n);
	}
	return (0);
}
