#include "holberton.h"

/**
 *reset_to_98 - shows the pointer of the var
 *@n: var for p and init
 *
 */

int _strlen(char *s)
{
	int length = 0;

	while (*s != '\0')
	{
		length++;
		s++;
	}
	return (length);
}
