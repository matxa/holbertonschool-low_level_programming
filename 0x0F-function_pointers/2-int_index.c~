#include <stdlib.h>
#include <stdio.h>
#include "function_pointers.h"

/**
 * int_index - find index
 * @array: arr
 * @size: size
 * @cmp: func
 * Return: index
 */

int int_index(int *array, int size, int (*cmp)(int))
{
	if (cmp)
}
