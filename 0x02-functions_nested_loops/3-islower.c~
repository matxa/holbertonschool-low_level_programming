#include "holberton.h"

/**
 * _islower - checks if c is lowercase
 * C is a variables
 * return 1 is c is lower case
 * Return: Always 0 (Success)
 */

int _islower(int c)
{
	if (_islower(c))
	{
		return (1);
	}
	return (0);
}
